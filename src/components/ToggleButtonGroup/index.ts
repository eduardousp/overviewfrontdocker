import { ToggleButtonGroupItem } from '@components/ToggleButtonGroup/ToggleButtonGroupItem';
import { ToggleButtonGroupRoot } from '@components/ToggleButtonGroup/ToggleButtonGroupRoot';

/**
 * Usage example:
 * ```jsx
 * <ToggleButton.Root>
 *   <ToggleButton.Item />
 * </ToggleButton.Root>
 * ```
 */
export const ToggleButtonGroup = {
  Root: ToggleButtonGroupRoot,
  Item: ToggleButtonGroupItem,
};
